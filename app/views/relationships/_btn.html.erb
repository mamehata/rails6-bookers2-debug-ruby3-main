<% if current_user.following?(user) %>
  <%= link_to "フォローする", user_relationships_path(user.id), class:"btn btn-success btn-sm", method: :delete %>
<% else %>
  <%= link_to "フォロー外す", user_relationships_path(user.id), class:"btn btn-info btn-sm", method: :post %>
<% end %>